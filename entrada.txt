{* Exemplo de Programa na linguagem L *}

integer n=1;
string nome;
const MAXITER=10;

write("Digite seu nome: ");
readln(nome);

real percentual;
while n[1]<=MAXITER begin
  percentual=real(n)/MAXITER;
  writeln(percentual,": Ola' ",nome);
  n=n+1;
end
